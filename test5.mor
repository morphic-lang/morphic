type InnerType {
  InnerData(Array Int),
}

type OuterType {
  OuterData(InnerType),
}

proc main(): () =
  let arr = [1, 2] in
  let _ = OuterData(InnerData(arr)) in
  do output("Completed all tests\n")